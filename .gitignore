AWS_DEPLOYMENT_GUIDE.md
LESSON.md
gemini.md
linkedin.md
future_updates.md

# ── Build / dependency artifacts ───────────────────────────────────
node_modules/
frontend-v2/.next/
frontend-v2/out/
.next/
out/

# ── Python ─────────────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/

# ── Local live data snapshot (produced by the Lambda, not source) ──
frontend-dashboard/latest_status.json
frontend-v2/public/latest_status.json
latest_status.json

# ── Build / deploy artifacts (generated from committed source) ────
lambda_deploy.zip

# ── Forecast engine runtime cache (downloaded history, not source) ──
forecast_cache/

# OS cruft
.DS_Store
